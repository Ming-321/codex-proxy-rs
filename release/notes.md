# v3.15.2+car.1

## 自用版更新

- 同步上游正式版 v3.15.2，包含连接失败安全重试、实际服务账号分组费用统计、客户端身份配置和插件宿主能力；不包含其后的未发版提交。
- 保留拼车管理、车位共享 RPM／并发／费用限制、份额分配、账号周期账本和自动额度预测保护。
- 拼车页面使用上游组件库；Key 客户端身份沿用上游通用配置，现有 OpenAI／xAI 覆盖保持兼容。

## 升级与恢复

- 适用于已有 car 定制版或空库初始化；0001–0021 迁移保持原样，上游插件等结构通过 0022 追加。官方版本数据库不能直接换成本镜像。
- 新增插件和账号预热能力不自动启用；既有拼车策略与业务数据保持。
- 部署前停止新流量、排空请求并保留完整数据库／配置备份及旧镜像。回退须使用已验证的配套数据库恢复方案，不能仅更换旧镜像或删除迁移记录；恢复前核对切换后的新增数据。
- 本定制版通过固定镜像升级，不使用上游在线更新替换定制程序。
